"PHP shortcuts
inoreabbrev <buffer> elsee else<cr>{<cr><cr>}<esc>ki

inoreabbrev <buffer> sout echo "";<esc>hi

inoreabbrev <buffer> ffor for($i = 0; $i < ; $i++)<cr>{<cr><cr>}<esc>kkk

inoreabbrev <buffer> iff if()<cr>{<cr><cr>}<esc>kkkllli
